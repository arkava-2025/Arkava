---
const { data, sectionMeta } = Astro.props;
const typename = sectionMeta?.content?.__typename ?? 'UnknownComponent';
---
<section class="py-12">
  <div class="mx-auto max-w-3xl rounded-md border border-dashed border-red-400 bg-red-50 p-6 text-red-800">
    <h2 class="text-lg font-semibold">Missing component mapping</h2>
    <p class="mt-2">No component was found for typename:
      <code class="rounded bg-white px-1 py-0.5 text-red-700">{typename}</code>
    </p>
    <details class="mt-3 text-sm">
      <summary class="cursor-pointer">View section data</summary>
      <pre class="mt-2 overflow-auto rounded bg-white p-3 text-xs text-gray-800">{JSON.stringify({ data, sectionMeta }, null, 2)}</pre>
    </details>
  </div>
</section>
