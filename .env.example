# Hygraph GraphQL endpoint (required)
# Example (replace with your actual project endpoint from Hygraph):
# HYGRAPH_ENDPOINT=https://api-us-east-1.hygraph.com/v2/<your-project-id>/master
HYGRAPH_ENDPOINT=

# Hygraph permanent auth token (optional, only if your project requires auth)
# HYGRAPH_TOKEN=your_hygraph_permanent_auth_token
